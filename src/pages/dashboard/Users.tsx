export function Users() {
    return (
      <div>
        <h1 className="text-3xl font-bold mb-4">Users</h1>
        <p>Manage users (hosts and admins) here.</p>
        {/* Add user management UI */}
      </div>
    );
  }