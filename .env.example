# API Configuration
VITE_API_URL=http://localhost:8000/api

# Paddle Environment (sandbox or production)
# This just tells the frontend which environment we're using - no secrets involved
VITE_PADDLE_ENVIRONMENT=sandbox
